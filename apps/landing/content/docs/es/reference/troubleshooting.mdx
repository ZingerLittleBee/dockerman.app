---
title: "Resolución de problemas"
description: "Problemas habituales y cómo resolverlos: conexión, permisos, rendimiento y Kubernetes."
---

Problemas habituales y cómo resolver cada uno.

## No se puede conectar al demonio de Docker

Elige tu plataforma:

<Tabs items={['macOS', 'Windows', 'Linux']}>
  <Tab>
    Comprueba que Docker Desktop esté en ejecución y revisa [Configuración → Ajustes de Docker](/es/docs/reference/settings#configuración-de-docker) para ver la ruta del socket que Dockerman debe usar.
  </Tab>
  <Tab>
    Elige entre Docker Desktop y el motor WSL2 en [Windows](/es/docs/platform/windows). Si Dockerman apunta al backend incorrecto, cámbialo en [Configuración → Ajustes de Docker](/es/docs/reference/settings#configuración-de-docker).
  </Tab>
  <Tab>
    Añade tu usuario al grupo docker y sigue las [instrucciones de permisos en Linux](/es/docs/platform/linux#permisos).
  </Tab>
</Tabs>

## Errores de permisos en Linux

Si Dockerman no puede acceder a `/var/run/docker.sock`, añade tu usuario al grupo docker, cierra sesión y vuelve a iniciarla, y verifica que `docker ps` funcione sin `sudo`.

## El Asistente de Configuración de WSL2 falla

Usa la sección de resolución de problemas de Windows para la ruta de WSL2: [Resolución de problemas en la plataforma Windows](/es/docs/platform/windows#solución-de-problemas).

## La conexión al host SSH falla

Comprueba los datos de conexión y si alguna regla del firewall bloquea el tráfico saliente por el puerto 22. El proceso de configuración completo está en [Hosts remotos](/es/docs/advanced/remote-hosts).

## Problemas de rendimiento

- Un consumo alto de CPU en reposo suele significar que una pestaña de Registros está transmitiendo un conjunto de archivos grande. Pausa las pestañas que no estés usando.
- Si las listas con cientos de contenedores cargan despacio, filtra primero por estado o por proyecto Compose.
- Si las estadísticas van lentas, usa un rango existente más corto, por ejemplo 1h en vez de 6h o 24h.

## Kubernetes: k3d no se inicia

<Accordions>
  <Accordion title="El puerto 6443 ya está en uso">
    Detén la otra herramienta de Kubernetes o cambia el puerto API de k3d antes de intentarlo de nuevo.
  </Accordion>
  <Accordion title="vm.max_map_count demasiado bajo">
    Ejecuta `sudo sysctl -w vm.max_map_count=262144` y persiste el valor en `/etc/sysctl.conf` si quieres que sobreviva al reinicio.
  </Accordion>
  <Accordion title="Falló la descarga del binario de k3d">
    Comprueba el acceso HTTPS saliente y reintenta la descarga. Si hace falta, coloca el binario a mano en la ruta que muestra el error.
  </Accordion>
</Accordions>

## Kubernetes: falla la importación de kubeconfig

- Selecciona el nombre de contexto correcto cuando importes el kubeconfig.
- Actualiza las credenciales caducadas y exporta el kubeconfig de nuevo.
- Comprueba que `kubectl get ns` funcione desde la línea de comandos antes de importar detrás de un proxy o firewall.

## Recopilación de registros

Abre primero la página de Registro de la Aplicación. Si necesitas reportar un problema, exporta el Registro de la Aplicación y adjúntalo junto con la versión de Dockerman.

## Obtener ayuda

Adjunta el App Log y la versión del paquete o release. No existe Configuración → Acerca de.
