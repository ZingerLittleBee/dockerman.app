---
title: "Linux"
description: "Linux ディストリビューションに Dockerman をインストールし、セットアップ用に Docker Engine を構成。"
---

Dockerman は `.deb`、`.rpm`、または AppImage として出ているので、お使いのディストリビューションに入れられます。

## インストール

<Tabs items={['.deb', '.rpm', 'AppImage']}>
  <Tab>
    ```bash
    sudo dpkg -i Dockerman_*_amd64.deb
    sudo apt-get install -f
    ```

    DebianまたはUbuntuに`.deb`パッケージをインストールします。
  </Tab>
  <Tab>
    ```bash
    sudo rpm -i Dockerman-*-1.x86_64.rpm
    # or
    sudo dnf install Dockerman-*-1.x86_64.rpm
    ```

    FedoraまたはRHELに`.rpm`パッケージをインストールします。
  </Tab>
  <Tab>
    ```bash
    chmod +x Dockerman_*_amd64.AppImage
    ./Dockerman_*_amd64.AppImage
    ```

    AppImage は直接実行します。Dockerman 固有の `--install` オプションはありません。
  </Tab>
</Tabs>

## Docker Engine のセットアップ

[docs.docker.com/engine/install/](https://docs.docker.com/engine/install/) の公式手順に従い、デーモンが実行されていることを確認してください。Dockerman は、個別に検証した Docker Engine の最低バージョンを現在公開していません。

## 権限

<Steps>
  <Step>
    ### docker グループへのユーザ追加

    ```bash
    sudo usermod -aG docker $USER
    ```
  </Step>
  <Step>
    ### ログアウトと再ログイン

    グループメンバーシップは新しいログインセッションにのみ適用されます。
  </Step>
  <Step>
    ### アクセスの確認

    `sudo`なしで`docker ps`を実行してください。
  </Step>
</Steps>

<Callout type="warn">
  `docker` グループに入ることは、ホスト上で事実上 root と同じ権限を持つことです。より厳しい境界が必要なら rootless Docker を使ってください。
</Callout>

## ディスプレイサーバー

Dockerman は Wayland または X11 で実行できます。グローバルショートカットの挙動はデスクトップ環境と Tauri/WebView の統合に依存し、Dockerman は独自の xdg-desktop-portal ショートカットバックエンドを実装していません。

## Freedesktop 統合

Dockerman は `.desktop` ファイルを入れるので、アプリケーションメニューに出ます。トレイアイコンは StatusNotifierItem 仕様で、KDE、拡張機能付き GNOME、XFCE、MATE で動きます。

## トラブルシューティング

<Accordions>
  <Accordion title="docker コマンドが見つからない">
    まず Docker Engine を入れ、新しいシェルを開いて PATH が Docker CLI を見つけるようにしてください。
  </Accordion>
  <Accordion title="/var/run/docker.sock でのアクセス権拒否">
    ユーザを`docker`グループに追加し、ログアウトして再ログインして新しいグループメンバーシップを有効にしてください。
  </Accordion>
  <Accordion title="トレイアイコンが表示されない">
    デスクトップ環境用のStatusNotifierItem拡張機能をインストールしてください。GNOMEでは通常、AppIndicatorとKStatusNotifierItem Supportが必要です。
  </Accordion>
</Accordions>
