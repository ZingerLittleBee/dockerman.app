---
title: "卷"
description: "管理 Docker 卷 — 创建、检查和清理持久化存储。"
---

用这一页列出、创建、检查和清理卷。要浏览卷内容时，跳到[文件](/zh/docs/docker/files)。

## 卷列表

卷列表显示名称、驱动、挂载点、大小和创建时间。用排序和筛选找到目标卷。

## 创建卷

<Steps>
  <Step>
    ### 选择名称和驱动

    选择卷名称以及对话框当前提供的选项（`local`、`nfs` 或 `cifs`）。该列表不会根据已安装的 Docker 插件动态生成。
  </Step>
  <Step>
    ### 驱动选项（可选）

    添加任何驱动特定的选项，例如 local 驱动的 NFS 挂载设置。
  </Step>
  <Step>
    ### 标签（可选）

    以后要分组或查询卷时，加上标签。
  </Step>
</Steps>

## 浏览卷文件

使用[文件](/zh/docs/docker/files)浏览卷的内容、预览文件、编辑文本文件、上传文件、下载文件以及处理多选操作。

## 检查卷

打开检查以查看卷 JSON，包括挂载点、驱动选项、标签、作用域以及当前正在使用该卷的容器列表。

## 清理未使用的卷

清理会把操作交给所选运行时，由运行时判断哪些卷未使用。继续前请核对确认对话框。

<Callout type="warn">
  清理会删除所有未使用的卷，包括你可能想要保留的命名卷。要单独删某个卷时，请改用删除功能。
</Callout>
